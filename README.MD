___Vim Configuration directory___

Note: The vimrc file is in the root of ~/.vim.

This will work for vim version 7.4


Otherwise, symlink ~/.vim/vimrc to ~/.vimrc

Don't forget to git submodule update --init
